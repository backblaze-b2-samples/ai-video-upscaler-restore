# Heavy ML restoration stack — installed ONLY when you actually want to RUN
# restorations. The base requirements.txt has everything needed for the API,
# the UI, all B2 features, tests, and lint to work WITHOUT this stack. The
# engine is lazy-imported (see app/repo/upscaler.py), so nothing here is
# imported at startup or during the test suite.
#
#   pip install -r services/api/requirements-restore.txt
#
# basicsr (pulled in by realesrgan/gfpgan) imports
# `torchvision.transforms.functional_tensor`, which was removed in torchvision
# >= 0.17. app/repo/upscaler.py installs a tiny shim aliasing the old module
# path to torchvision.transforms.functional before importing the engine, so
# patched torch/torchvision releases remain compatible with the engine.

torch>=2.13.0,<2.14.0
torchvision>=0.28.0,<0.29.0
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.9.0
realesrgan>=0.3.0
gfpgan>=1.3.8
basicsr>=1.4.2
imageio-ffmpeg>=0.4.9
